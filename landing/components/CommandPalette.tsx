'use client'
import { useEffect, useMemo, useState } from 'react'
const actions=[{label:'Explore deals',hint:'Browse the published opportunity feed',href:'/deals',keys:['deals','explore','opportunities','map']},{label:'Compare deals',hint:'Review two or three opportunities side by side',href:'/compare',keys:['compare','decision','shortlist']},{label:'My DealScan',hint:'Open your saved research workspace',href:'/my-dealscan',keys:['saved','workspace','my dealscan']},{label:'Home',hint:'Return to the DealScan overview',href:'/',keys:['home','overview']}]
export default function CommandPalette(){const[open,setOpen]=useState(false),[query,setQuery]=useState(''),[active,setActive]=useState(0);const filtered=useMemo(()=>{const q=query.trim().toLowerCase();return q?actions.filter(a=>`${a.label} ${a.hint} ${a.keys.join(' ')}`.toLowerCase().includes(q)):actions},[query]);useEffect(()=>{const onKeyDown=(e:KeyboardEvent)=>{const command=e.metaKey||e.ctrlKey;if(command&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(true);setQuery('');setActive(0);return}if(!open)return;if(e.key==='Escape')setOpen(false);if(e.key==='ArrowDown'){e.preventDefault();setActive(v=>Math.min(v+1,Math.max(filtered.length-1,0)))}if(e.key==='ArrowUp'){e.preventDefault();setActive(v=>Math.max(v-1,0))}if(e.key==='Home'){e.preventDefault();setActive(0)}if(e.key==='End'){e.preventDefault();setActive(Math.max(filtered.length-1,0))}if(e.key==='Enter'&&filtered[active])window.location.href=filtered[active].href};window.addEventListener('keydown',onKeyDown);return()=>window.removeEventListener('keydown',onKeyDown)},[open,filtered,active]);return <><button type="button" onClick={()=>{setOpen(true);setQuery('');setActive(0)}} className="fixed bottom-5 right-5 z-40 hidden items-center gap-2 rounded-full border border-[#dbe5df] bg-white/90 px-3.5 py-2.5 text-xs font-bold text-[#536159] shadow-[0_12px_35px_rgba(31,52,42,.10)] backdrop-blur-xl transition hover:-translate-y-0.5 hover:border-[#b9cfc1] hover:text-[#176b45] sm:flex" aria-label="Open command search"><span aria-hidden="true" className="grid h-6 w-6 place-items-center rounded-lg bg-[#edf5f0] text-[#176b45]">⌘</span>Search DealScan<kbd className="rounded-md border border-[#e2e8e4] bg-[#f7f9f7] px-1.5 py-0.5 font-mono text-[10px] text-[#89938e]">K</kbd></button>{open&&<div className="fixed inset-0 z-[70] flex items-start justify-center bg-[#102018]/20 px-4 pt-[12vh] backdrop-blur-[3px]" onMouseDown={e=>{if(e.currentTarget===e.target)setOpen(false)}}><div role="dialog" aria-modal="true" aria-label="DealScan command search" className="w-full max-w-xl overflow-hidden rounded-[1.5rem] border border-white/80 bg-white/95 shadow-[0_30px_100px_rgba(16,32,24,.20)] backdrop-blur-2xl animate-nav-in"><div className="flex items-center gap-3 border-b border-[#e7ece9] px-5 py-4"><span aria-hidden="true" className="text-lg text-[#8a958f]">⌕</span><input id="dealscan-command-search" autoFocus value={query} onChange={e=>{setQuery(e.target.value);setActive(0)}} placeholder="Search DealScan…" aria-label="Search DealScan destinations" aria-controls="dealscan-command-list" className="min-w-0 flex-1 bg-transparent text-sm font-semibold text-[#15211b] outline-none placeholder:text-[#a0aaa4]"/><button type="button" onClick={()=>setOpen(false)} className="rounded-lg border border-[#e2e8e4] px-2 py-1 font-mono text-[10px] text-[#89938e] hover:bg-[#f5f8f6]" aria-label="Close command search">ESC</button></div><div id="dealscan-command-list" role="listbox" aria-label="DealScan destinations" className="p-2">{filtered.length?filtered.map((a,i)=><a key={a.href} href={a.href} role="option" aria-selected={i===active} onMouseEnter={()=>setActive(i)} className={`flex items-center justify-between gap-4 rounded-xl px-3.5 py-3.5 transition ${i===active?'bg-[#edf5f0]':'hover:bg-[#f7f9f7]'}`}><div className="min-w-0"><div className="text-sm font-black text-[#1b2a22]">{a.label}</div><div className="mt-0.5 truncate text-xs text-[#7b8780]">{a.hint}</div></div><span aria-hidden="true" className="shrink-0 text-[#a0aaa4]">→</span></a>):<div className="px-4 py-10 text-center" role="status"><div className="text-sm font-bold text-[#536159]">No DealScan destination found</div><p className="mt-1 text-xs text-[#8a958f]">Try “deals”, “compare”, “saved”, or “home”.</p></div>}</div><div className="flex items-center justify-between border-t border-[#e7ece9] bg-[#fafcfb] px-5 py-3 text-[10px] font-semibold text-[#89938e]"><span>Navigate with ↑ ↓</span><span>Enter to open</span></div></div></div>}</>}
